<%= form_with(model: @chat_room, remote: true, class: "d-flex" ) do |f| %>
  <%= f.text_field :name, class: "", autocomplete: 'off' %>
  <%= f.submit data: { disable_with: false }, class: "border bg-gray-100 hover:bg-gray-300 p-2 rounded-md"%>
<% end %>